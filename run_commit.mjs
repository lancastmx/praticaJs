import { run } from 'file:///d:/aprenderProgramacion/praticaJs/src/agents/AutoCommitSkill.js';

run().then(result => {
    console.log("Run result:", result);
}).catch(err => {
    console.error("Run error:", err);
});
